---
const { title, text, text2, link, nameLink, icon, icon_name, url_maps } = Astro.props;
---

<div class="mx-3 md:mx-0 p-7 bg-white border border-gray-200 rounded-lg shadow-md">
  <img class="w-10 m-auto" src={icon} alt={icon_name} />

  <h5 class="mb-2 text-2xl font-semibold tracking-tight">
    {title}
  </h5>

  <p class="mb-3 font-light text-gray-500">
    {text}
    <br />
    {text2}
  </p>
    <iframe class="container m-auto rounded-lg w-full md:h-96" src={url_maps} style="border:0;" allowfullscreen="" loading="lazy" referrerpolicy="no-referrer-when-downgrade"></iframe>
  
</div>
